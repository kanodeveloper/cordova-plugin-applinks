package com.kanoapps.cordova.applinks;

import org.apache.cordova.*;

/**
 * Cordova plugin that allows for an arbitrarly sized and positioned WebView to be shown ontop of the canvas
 */
public class Applinks extends CordovaPlugin {

    private static final String TAG = "Applinks";

    @Override
    public void initialize(CordovaInterface cordova, CordovaWebView webView) {
        super.initialize(cordova, webView);
    }
}